CREATE TABLE TEMP_USER_TEST (
    id NUMBER GENERATED BY DEFAULT AS IDENTITY,
    name VARCHAR2(255),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    CONSTRAINT TEMP_USER_TEST_pk PRIMARY KEY (id)
);
